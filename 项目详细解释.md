# 中国企业招聘信息平台 — 完全新手版解释

> 写给没有任何编程基础的人。每个概念都用生活类比来解释。

---

## 一、这个项目是什么？

你可以把它理解成一本**电子版的企业招聘黄页**。

- 以前找工作，你要一个个搜"阿里巴巴招聘"、"腾讯校招"，打开几十个网页来回翻
- 现在打开这个软件，111 家公司的信息都在一个界面里：公司名、官网链接、招聘页面、招什么岗位、在哪些城市

打个比方：这就像把 111 家公司的招聘广告剪下来，贴在一张大纸上，你可以快速浏览，也可以按关键词搜索。

---

## 二、看到的效果是怎样的？

```
┌─────────────────────────────────────────────────┐
│  [图标] 中国企业招聘信息平台           ★ 收藏(3)  │
├─────────────────────────────────────────────────┤
│  [🔍 搜索框：输入"算法"就能筛选]                  │
│  [全部] [互联网] [AI] [游戏] [银行] ... ← 分类标签 │
│  找到 42 家公司                       [排序 ▼]    │
├────────────┬────────────┬────────────┬───────────┤
│ ★ 阿里巴巴  │ 腾讯        │ 字节跳动    │ 百度      │
│ 互联网      │ 互联网      │ 互联网      │ 互联网    │
│ 杭州/北京   │ 深圳        │ 北京/上海   │ 北京      │
│ 后端开发    │ 游戏开发     │ 算法工程师   │ AI研究员  │
│ 算法工程师  │ 后端开发     │ 后端开发     │ 自动驾驶  │
│ [官网] [投递]│ [官网] [投递]│ [官网] [投递] │ [官网] [投递]│
├────────────┴────────────┴────────────┴───────────┤
│  数据来源：各公司官网 · 仅供学习参考               │
└─────────────────────────────────────────────────┘
```

每一张卡片就是一个公司。点"官网"跳转到公司主页，点"投递"跳转到招聘页面。看到感兴趣的可以点星星收藏，下次打开还在。

---

## 三、为什么要做这个？

计算机专业的学生找工作，最大的问题不是"能力不够"，而是**信息闭塞**。

大多数同学只知道阿里、腾讯、字节、百度这几家大厂。但实际上招聘计算机岗位的公司远远不止这些：

- 工商银行总行有软件开发中心（年薪不低，还很稳定）
- 比亚迪在招自动驾驶工程师
- 米哈游在招游戏开发
- 微软中国在北京和苏州都有研发团队
- 宁德时代在招电池管理系统程序员

你根本不知道这些公司在招人，当然也就不会去投简历。这个项目就是想解决这个问题——**先把有哪些公司在招计算机学生这件事告诉你**。

---

## 四、里面的文件都是干什么的？

整个项目就像一个**房子的施工文件**。我逐一解释每个文件在"盖房子"中扮演什么角色。

---

### 第一类：你实际双击打开的那个东西（最终产品）

```
企业招聘信息平台_安装包.exe  ← 安装版，像装QQ一样装好
企业招聘信息平台.exe        ← 免安装版，双击直接打开
企业招聘信息平台.hta        ← 网页壳版本（微信能发的备用）
中国企业招聘信息平台.zip    ← 压缩包版本（微信能发）
```

**什么是 .exe 文件？**
就是你电脑上所有软件的格式。QQ 是 QQ.exe，微信是 WeChat.exe，Chrome 是 chrome.exe。双击 .exe，电脑就会启动一个程序。这个项目最终产出的就是一个 .exe 文件。

**什么是"安装版"和"免安装版"的区别？**
- 安装版：像装 QQ 一样，点下一步→下一步→完成，桌面上自动出现图标，开始菜单里也有
- 免安装版：不需要装，双击就打开，但要自己手动拖到桌面才能创建快捷方式

**什么是 .hta？**
一种 Windows 内置的网页应用格式。本质上就是一个网页，但看起来像独立软件（有窗口边框、任务栏图标，没有浏览器的地址栏）。它的优点是体积非常小（101KB），微信不会拦截。

**为什么微信发不了 .exe？**
微信为了防病毒，禁止直接发送 .exe 文件。解决方法是：把它塞进一个 .zip 压缩包里再发，微信就放行了。就像你不能带液体上飞机，但托运就可以。

---

### 第二类：做界面的文件（页面长什么样）

```
src/
├── index.html          ← 页面的"骨架"
├── css/
│   └── style.css       ← 页面的"皮肤和衣服"
├── js/
│   ├── data.js         ← 页面的"内容"（111家公司的数据）
│   ├── app.js          ← 页面的"大脑"（搜索、筛选、收藏等操作）
│   └── utils.js        ← 页面的"工具箱"（一些辅助功能）
└── assets/
    └── icon.svg        ← 页面的"头像"（Logo图标）
```

---

#### `index.html` — 页面的骨架

**通俗理解：**
盖房子先要搭钢筋框架。哪个位置放标题，哪个位置放搜索框，哪个位置放公司卡片——这些结构由这个文件决定。它本身没有颜色、没有数据、没有交互，只是一个空架子。

**为什么是 .html 格式？**
因为浏览器（Chrome、Edge、QQ浏览器等）最擅长展示的就是 .html 文件。你平时看的所有网页，本质上都是 .html 文件。

---

#### `style.css` — 页面的皮肤和衣服

**通俗理解：**
骨架搭好了，但光有骨架很丑。这个文件就是给骨架"穿上衣服"——标题用多大字、卡片用圆角还是直角、背景用什么颜色、鼠标移上去有什么动画效果，都是它说了算。

**为什么只有 3 种颜色？**
这是一个设计原则：颜色越少，越不容易难看。你想想那些五彩斑斓的传单——就是因为用了太多颜色，看起来廉价。这个项目只用 3 种颜色：

| 颜色 | 用在哪里 | 为什么 |
|------|---------|--------|
| 绿色 | 按钮、标签、星星 | 代表"可以去点"、"可以操作" |
| 白色 | 背景 | 干净、不刺眼 |
| 深蓝灰色 | 文字、边框 | 比纯黑色柔和，适合长时间看 |

---

#### `data.js` — 111 家公司的数据

**通俗理解：**
这就是一本通讯录，只不过每条记录不是姓名+电话，而是公司名+招聘网站+岗位类型+工作城市。

每条数据长这样（翻译成人话）：

```
公司：阿里巴巴
官网：alibaba.com
投简历的网址：talent.alibaba.com
校招专门网址：campus.alibaba.com
招什么岗位：后端开发、前端开发、算法工程师、产品经理、数据分析
在哪些城市有岗位：杭州、北京、深圳、上海、广州、成都
Fortune 排名：第 23 名
现在在招人吗：是的
有校招吗：有
```

这些信息是从各公司的官网招聘页面搜集整理出来的，一共 111 家，覆盖：
- 互联网公司（阿里、腾讯、字节、百度...）
- 手机/硬件公司（华为、小米、OPPO、大疆...）
- 游戏公司（米哈游、莉莉丝、鹰角...）
- AI 公司（商汤、旷视、科大讯飞...）
- 银行科技部门（工商银行、招商银行、建设银行...）
- 车企（比亚迪、蔚来、小鹏、理想...）
- 外企中国研发中心（微软、Google、苹果、英伟达...）

---

#### `app.js` — 页面的"大脑"

**通俗理解：**
数据有了，界面有了，但怎么让它们互动呢？你在搜索框里打字，怎么把不匹配的公司隐藏掉？你点了一个分类标签，怎么只显示这一类公司？你点星星收藏，下次打开怎么还记得？

这些"操作逻辑"全在 `app.js` 里。它就像一个管家：

1. 你打字搜索 → 管家从 111 家公司里筛出名字匹配的 → 重新显示
2. 你点了"游戏"标签 → 管家筛出所有游戏公司 → 重新显示
3. 你点了星星 → 管家把这家公司放进收藏夹 → 更新收藏数量
4. 你点了排序 → 管家按你的选择重新排列 → 重新显示

**收藏夹是怎么记住的？**
浏览器内置了一个叫"localStorage"的储物柜。你可以把数据放进去，关掉浏览器再打开，东西还在。就像你在手机备忘录里写了一条笔记，关机重启还在——localStorage 就是浏览器的备忘录。

---

#### `utils.js` — 工具箱

**通俗理解：**
一些反复用到的小功能，单独抽出来放工具箱里，避免重复写。

- 收藏夹的存取读写（就像工具箱里的螺丝刀——收藏功能到处用到它）
- "防抖"功能：你在搜索框里快速打字时，每按一个键不会立刻搜索，而是等你停下来 0.3 秒后再搜。否则你打"阿里巴巴"四个字会搜 4 次，浪费计算

---

#### `icon.svg` — Logo 图标

**通俗理解：**
软件总得有个图标吧。这个文件就是一个 64×64 像素的小图片，显示在标题旁边。

**什么是 SVG？**
你平时见的图片（JPG、PNG）是"位图"——由一个个像素点组成，放大会模糊。SVG 是"矢量图"——由数学公式描述形状，放大 100 倍也不会模糊。用 SVG 是因为它在各种屏幕上都清晰。

---

### 第三类：打包文件（怎么把网页变成 .exe）

```
src-tauri/
├── tauri.conf.json    ← 打包说明书
├── Cargo.toml         ← 材料清单
├── build.rs           ← 自动执行脚本
├── src/
│   ├── main.rs        ← 程序的真正起点
│   └── lib.rs         ← 启动时要做的事情
├── icons/             ← 各个尺寸的图标
└── capabilities/
    └── default.json   ← 安全权限声明
```

---

**先说一个核心概念：网页是怎么变成软件的？**

你的界面是用 HTML/CSS/JS 写的——这些本来是给浏览器看的。要把它们变成 .exe，需要一个"翻译官"。

这个翻译官做了三件事：
1. 创建一个真正的 Windows 窗口（有边框、标题栏、任务栏图标）
2. 在窗口里嵌入一个浏览器内核（相当于藏了一个隐形的 Edge 浏览器）
3. 让这个隐形浏览器打开你的 HTML 页面

用户看到的是一个正常的软件窗口，但里面显示的东西其实是你的网页。这个"翻译官"就是 Tauri。

| 方案 | 大小 | 比喻 |
|------|------|------|
| **我们用的 Tauri** | 11MB | 叫个小快递员（借用系统自带的 Edge 内核） |
| 如果用了 Electron | 150MB+ | 开辆大卡车（自带一整个 Chromium 浏览器） |

Tauri 为什么小？因为它借用了 Windows 系统自带的"Edge 内核"来显示网页。就像你去朋友家做客不用自带电视——朋友家有。Electron 则是走到哪都背着一台电视。

---

#### `tauri.conf.json` — 打包说明书

告诉 Tauri：
- 你叫什么名字（显示在窗口标题栏）
- 窗口默认多大（宽 1280、高 800 像素）
- 图标在哪
- 包成什么样的安装文件（选 NSIS 格式）

---

#### `Cargo.toml` — 材料清单

Rust 语言的项目配置文件。列出需要用到哪些"现成的零件"：
- tauri：窗口管理框架
- tauri-plugin-shell：用来在系统浏览器打开链接（比如用户点"官网"，要用 Edge 打开，不能关在软件里打开）

**什么是"Cargo"？** 它是 Rust 语言的包管理工具，类比手机上的应用商店。你告诉它"我需要这些零件"，它自动帮你下载安装。

---

#### `main.rs` — 程序的真正起点

这个文件只有 5 行。它的唯一工作是：启动程序，然后调用 `lib.rs`。

```
打开 .exe → main.rs 启动 → 隐藏黑窗口 → 调用 lib.rs
```

`#![windows_subsystem = "windows"]` 这行的意思是：不要弹出一个黑色的命令行窗口。没有这行，用户双击 .exe 会先看到一个黑框，体验很差。

---

#### `lib.rs` — 启动时做两件事

1. 加载 Tauri 框架
2. 注册 shell 插件（这样用户点链接才能在系统浏览器打开）

然后 Tauri 自动完成剩下的：创建窗口 → 加载 HTML → 显示给用户。

---

#### `capabilities/default.json` — 安全权限声明

这个文件告诉 Windows：这个软件只需要以下两个权限：
1. 基本的窗口操作（打开、关闭、缩放）
2. 在系统浏览器打开网页链接

不会碰你的文件、不会访问你的摄像头、不会读你的联系人。相当于一张"权限声明书"。

---

#### `icons/` — 各种尺寸的图标

Windows 在不同地方用不同大小的图标：
- 任务栏：32×32 像素
- 桌面：128×128 像素
- 4K 屏幕桌面：256×256 像素
- 文件资源管理器：各种尺寸

如果只给一张图，Windows 会强行压缩或拉伸，导致模糊。提供所有尺寸，各用各的，永远清晰。

---

### 第四类：项目文档（给自己和别人看的说明）

```
docs/                    ← 这些文档都是写给人看的，不是给电脑看的
├── requirements.md      ← 我想做什么、为什么要做
├── architecture.md      ← 技术怎么实现、为什么选这个技术
├── design-spec.md       ← 长什么样、什么颜色多少像素
└── implementation-steps.md ← 一步步做了哪些事

开发日志.md              ← 每天干了什么的记录
CLAUDE.md               ← 给 AI 助手的工作指南
README.md               ← 给人类看的快速入门
项目详细解释.md           ← 就是你现在看的这份文件
```

通俗理解：代码是给电脑看的，文档是给人看的。你三个月后回来看这个项目，代码可能已经看不懂了（很正常），但文档能帮你回忆起当时做了什么、为什么这么做。

---

### 第五类：辅助工具文件

```
build.bat           ← 一键重新生成 .exe
.gitignore          ← 告诉 Git 哪些文件不用存
package.json        ← 用到了哪些 Node.js 零件的清单
```

---

#### `build.bat` — 一键重新生成 .exe

如果你改了代码（比如加了新公司数据），双击 `build.bat` 就会自动重新编译出新的 .exe。就像面包机——放原料进去，按一个键，吐司出来。

它里面做的事情：
1. 告诉电脑 VS 2022 的工具在哪（编译需要用到 VS 的"链接器"）
2. 告诉电脑 Windows SDK 在哪（打包需要用到 Windows 的开发工具）
3. 运行 `npx tauri build`（Tauri 的打包命令）
4. 完事

**什么是"链接器"？**
你可以把写代码想象成做菜：
- 程序员写代码 = 准备食材
- 编译器 = 切菜
- 链接器 = 把切好的菜组合成一盘菜（.exe 文件）

VS 2022（Visual Studio 2022）是微软出的开发工具套装，链接器是它的一部分。D 盘装的那个 VS 就是为了提供这个链接器。

---

#### `.gitignore` — 告诉 Git 哪些文件不用存

**什么是 Git 和 GitHub？**
- Git：一个"版本记录器"。你每次修改代码，Git 都会拍一张快照，哪天改坏了可以退回去。就像游戏存档。
- GitHub：一个"存档的云盘"。把 Git 的记录上传到网上，换台电脑也能继续工作，别人也能看到你的代码。

`.gitignore` 告诉 Git：.exe 文件不要存（太大了）、.zip 文件不要存、安装的零件（node_modules）不要存——这些都可以重新生成，存了浪费空间。

---

## 五、不要被名词吓到

你可能在文档里看到很多不懂的词。没关系，每个程序员都是从不懂开始的。下面是出现过的名词，用一句话解释：

| 名词 | 一句话解释 |
|------|-----------|
| HTML | 网页的骨架，决定"有什么" |
| CSS | 网页的衣服，决定"长什么样" |
| JavaScript | 网页的大脑，决定"能干什么" |
| .exe | Windows 上可双击打开的程序文件 |
| Rust | 一种编程语言，用来写 Tauri 的窗口管理部分 |
| Tauri | 把网页变成桌面软件的工具 |
| WebView2 | Edge 浏览器的内核，Tauri 用它来显示网页 |
| VS 2022 | 微软的开发工具套装，提供编译 .exe 需要的工具 |
| npm | Node.js 的零件商店，帮你下载和管理代码零件 |
| Git | 版本记录器，记录你每次改了什么 |
| GitHub | 代码托管网站，Git 记录的云端备份 |
| SVG | 矢量图片格式，放大不会模糊 |
| localStorage | 浏览器的储物柜，数据关掉再开还在 |
| NSIS | 一种安装包制作工具，生成安装向导 |

---

## 六、整个项目的制作过程

把这个项目从零到有，大概经历了这些步骤：

```
第 1 步：确定做什么
  → 写需求文档（docs/requirements.md）

第 2 步：做网页版
  → 收集 111 家公司数据 → 写页面 → 写样式 → 写搜索筛选功能

第 3 步：在浏览器里调试
  → 用 index.html 打开，看看效果对不对

第 4 步：变成 Windows 软件
  → 装 Rust → 配置 Tauri → 编译成 .exe

第 5 步：做成安装包
  → 生成 NSIS 安装包 → 别人收到就能像装 QQ 一样装

第 6 步：写文档
  → README、架构说明、开发日志、项目解释（你正在看的）
```

前两步是"做内容"——决定这个软件里有什么信息、长什么样。
第三、四步是"做包装"——把网页变成双击就能打开的真正软件。
后两步是"做说明"——让其他人（包括三个月后的你自己）能理解这个项目。
