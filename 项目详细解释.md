# 中国企业招聘信息平台 — 项目详细解释

## 这个项目解决什么问题？

计算机专业学生找工作时最大的痛点不是"投了简历没人回"，而是连**哪些公司在招人**都不知道。大多数同学只知道阿里、腾讯、字节这几家，实际上招聘 CS 岗位的企业有上百家，包括银行科技部、车企自动驾驶团队、外企中国研发中心等等。

这个项目把 111 家企业的**公司名称、官网、招聘页面、校招入口、典型 CS 岗位、工作城市**聚合到一个地方，让找工作从"大海捞针"变成"精准筛选"。

---

## 整体架构

```
从代码 → 到软件
┌──────────────────────┐      ┌──────────────────────┐
│  src/                │      │  src-tauri/           │
│  前端（HTML/CSS/JS）  │ ──→ │  Rust 壳（打包成 .exe）│
│  浏览器里开发调试     │      │  Windows 原生窗口     │
└──────────────────────┘      └──────────────────────┘
        │                              │
        │          build.bat            │
        └──────── 一键构建 ─────────────┘
                      │
                      ▼
        ┌──────────────────────────┐
        │  企业招聘信息平台.exe      │
        │  企业招聘信息平台_安装包.exe│
        └──────────────────────────┘
```

---

## 每个文件夹/文件干了什么 + 为什么

---

### `src/` — 前端源代码（项目的灵魂）

这是你打开软件看到的界面的全部来源。包含 HTML 页面、CSS 样式、JavaScript 数据和逻辑。

---

#### `src/index.html` — 页面骨架

```
打开软件时看到的一切，都由这个文件来容纳。
```

它在 `<header>` 里放标题和收藏按钮，在 `<main>` 里放搜索框和卡片网格，在 `<footer>` 里放数据来源说明。

**为什么是 HTML 而不是 Vue/React？** 这个项目只需要展示数据，没有任何复杂的状态交互。用框架反而会增加 100KB+ 的运行时代码，而且会让 HTA 版本无法使用（框架依赖 `file://` 协议下的模块加载）。原生 HTML 加载速度最快，零依赖。

---

#### `src/css/style.css` — 样式表（约 600 行）

控制整个应用的外观，采用三层设计令牌体系：

```
:root {
  --green: #2ecc71;      ← CSS 变量，像 Excel 的样式引用
  --navy: #2c3e50;       ← 改一处颜色，整个应用跟着变
  --radius: 12px;        ← 统一圆角值
}
```

**为什么只有 3 种颜色？** 这是刻意约束。网页新手容易写 10 种颜色混在一起，很难看。强制 3 色（绿色 + 白色 + 海军蓝）倒逼出简洁统一的设计。深浅变体是通过透明度或亮度衍生出来的，不算新颜色。

**为什么用 CSS Grid 而不是 Flexbox？** 卡片网格需要自动适配屏幕宽度（4列→3列→2列→1列）。Grid 的 `repeat(auto-fill, minmax(290px, 1fr))` 一行代码就搞定了——不需要 JavaScript 监听 resize 事件、不需要手动计算列数。

---

#### `src/js/data.js` — 111 家企业数据集（约 78KB，最费时间的文件）

每一家企业是一个 JavaScript 对象，包含 14 个属性：

```javascript
{
  id: "alibaba",                    // 唯一标识（用于收藏功能）
  nameCN: "阿里巴巴",               // 中文名
  nameEN: "Alibaba Group",          // 英文名
  industry: "互联网",               // 主要行业
  categories: ["互联网", "电商"],    // 多标签（用于分类筛选）
  website: "https://...",           // 公司官网
  careerPage: "https://...",        // 社会招聘页面
  careerCampus: "https://...",      // 校园招聘入口
  positions: ["后端开发", ...],      // CS 相关岗位
  fortuneRank: 23,                  // Fortune 中国 500 排名
  hiring: true,                     // 当前是否招聘中
  hasCampusRecruit: true,           // 是否有校招
  cities: ["杭州", "北京", ...]      // 工作城市
}
```

**为什么数据写在 JS 文件里而不是 JSON 文件？** 浏览器的 CORS 安全策略禁止 `file://` 协议加载外部 JSON（`fetch('data.json')` 会报错）。而 `<script src="data.js">` 不受此限制——浏览器将其视为代码执行，不视为跨域请求。这是纯本地 HTML 应用加载数据的标准做法。

**为什么不用数据库？** 这个项目的约束是：不需要安装任何服务器软件。如果在本地跑 MySQL，发给同学他们根本用不了。111 条数据是完全静态的，JS 数组就是最合适的数据结构——加载零延迟，`Array.filter()` 查找是微秒级别的。

---

#### `src/js/app.js` — 核心应用逻辑（约 330 行）

承担所有交互功能：搜索、筛选、排序、渲染、收藏管理。

```
数据流（单向）：
用户操作 → 更新 state → filterAndRender() → renderCompanies() → 刷新屏幕
                ↑                                    │
                └────── 所有数据变化走这条路 ────────┘
```

**为什么用一个集中式 `state` 对象？** 当搜索词、分类、排序方式、校招开关、收藏视图五个条件同时影响显示结果时，如果每个条件各自触发渲染，会出现"闪烁"——先按 A 条件渲染，再按 B 条件覆盖。集中式 state 保证每次操作都是从完整状态重新计算，一次渲染到位。

**为什么用 IIFE 包裹？** `(function(){ ... })()` 创建了一个闭包，所有变量（state、DOM 引用）都在这个闭包内，不会污染全局作用域。这样做的好处是：即使以后在 HTML 里加其他脚本，也不会和 app.js 里的变量名冲突。

**为什么用 DocumentFragment？** 渲染 111 张卡片如果每次都直接 `appendChild` 到 DOM，浏览器会触发 111 次"回流"（重新计算布局）。DocumentFragment 是一个"离屏容器"——先把卡片都放进 fragment，最后一次添加到页面，只触发 1 次回流。对 111 条数据来说性能差异不明显，但当数据扩展到 500+ 时能感觉到明显卡顿。

---

#### `src/js/utils.js` — 工具函数（约 40 行）

三个职责：

1. **收藏夹 CRUD** — `getFavorites()` / `toggleFavorite()` / `isFavorite()` 操作 `localStorage`
2. **防抖函数** — `debounce(fn, delay)` 在搜索输入时使用，用户停止打字 300ms 后才触发搜索，避免每敲一个字都搜索一次
3. **城市格式化** — `formatCities()` 把 `["深圳","北京","上海","成都"]` 显示为 `深圳/北京/上海`（最多 3 个）

**为什么用 localStorage 而不是文件存储？** localStorage 是浏览器内置的键值库，数据永久保存。而且 Tauri 的 WebView2 也支持它——收藏的数据在 .exe 版本里同样能持久化。

---

#### `src/assets/icon.svg` — Logo 图标

一个 64×64 的 SVG 图标，绿色圆角矩形 + 白色列表符号 + 海军蓝勾号。在页面标题旁显示。

**为什么是 SVG 而不是 PNG？** SVG 是矢量格式——放大到 1 米宽也不会模糊，缩小到 16px 也清晰。PNG 在高分辨率屏幕上会有锯齿。

---

### `src-tauri/` — Rust 后端（把网页包成 .exe）

Tauri 做的事情：创建一个原生 Windows 窗口 → 在窗口里嵌入 WebView2（Edge 浏览器内核） → 加载 HTML 页面。

#### `src-tauri/tauri.conf.json` — 应用配置

```json
{
  "productName": "中国企业招聘信息平台",  // 任务栏和标题栏上的名字
  "identifier": "com.jobfinding.desktop", // 唯一ID，用于系统区分应用
  "build": {
    "frontendDist": "../src"              // 告诉 Tauri 前端代码在 ../src/
  },
  "app": {
    "windows": [{
      "width": 1280, "height": 800,       // 窗口默认尺寸
      "resizable": true,                   // 允许用户拉伸窗口
      "center": true                       // 启动时在屏幕居中
    }]
  },
  "bundle": {
    "targets": ["nsis"],                   // 构建 NSIS 安装包
    "icon": ["icons/icon.ico", ...]        // 应用图标
  }
}
```

#### `src-tauri/Cargo.toml` — Rust 依赖

告诉 Rust 编译器需要下载哪些库（tauri 框架本体、shell 插件用于打开外部链接、序列化库）。

#### `src-tauri/src/main.rs` + `lib.rs` — Rust 代码（共 20 行）

```
main.rs:   启动 → 隐藏控制台窗口（#![windows_subsystem = "windows"]）
lib.rs:    初始化 Tauri → 注册 shell 插件 → 运行应用
```

**为什么 Rust 代码这么少？** 因为这个应用全是前端逻辑——搜索、筛选、渲染都在 JavaScript 里。Rust 只负责"开一个窗口，把网页放进去"。Tauri 的设计哲学是：前端做 UI，Rust 只做系统调用（窗口管理、文件读写等），不写多余代码。

#### `src-tauri/capabilities/default.json` — 权限声明

声明应用拥有的系统权限。这里只申请了 `core:default`（基本窗口操作）和 `shell:allow-open`（允许在系统浏览器打开链接）。如果用户点"官网"按钮，这个权限让链接在 Edge/Chrome 中打开，而不是在应用内。

#### `src-tauri/icons/` — 多尺寸图标

```
icon.ico  → Windows 任务栏、桌面快捷方式、文件资源管理器
icon.png  → 安装包封面
32x32.png / 128x128.png / 128x128@2x.png → 各种分辨率场景
```

**为什么有这么多尺寸？** Windows 在不同场景用不同尺寸：任务栏要 32px，桌面要 128px，高 DPI 屏幕要 256px。如果只提供一个小图标，Windows 会强制拉伸导致模糊。提供所有尺寸让系统在每种场景都显示清晰。

---

### `docs/` — 项目开发文档

| 文件 | 内容 |
|------|------|
| `requirements.md` | 需求文档：用户是谁、要解决什么问题、优先级怎么排 |
| `architecture.md` | 技术架构：为什么选这些技术、数据怎么流动、有哪些设计模式 |
| `design-spec.md` | 设计规范：配色、字号、间距、圆角、动画参数的精确数值 |
| `implementation-steps.md` | 实施步骤：从脚手架到打包分发，8 个阶段的完成情况 |

**为什么需要这些文档？** 代码告诉你"是什么"，文档告诉你"为什么"。过了三个月再回来看代码，你会忘了当时为什么用 Grid 而不用 Flexbox——架构文档里记着决策理由。如果要带其他同学一起做，文档让他们不用反复问。

---

### 根目录文件

| 文件 | 作用 |
|------|------|
| `CLAUDE.md` | AI 助手（Claude Code）的工作指引，包含文件地图和工作约定 |
| `README.md` | 给人类看的项目说明，快速开始 + 功能列表 |
| `开发日志.md` | 每日开发记录，完成事项 + 待办 |
| `项目详细解释.md` | **本文档**，解释每个文件为什么存在 |
| `build.bat` | 一键构建脚本，配好 VS 环境变量后双击即可编译 .exe |
| `.gitignore` | 告诉 Git 哪些文件不要提交（构建产物、.exe、.zip） |
| `package.json` | Node.js 项目配置，包含 Tauri CLI 和图标生成工具依赖 |

---

### 分发文件（`.gitignore` 排除，不提交到 GitHub）

| 文件 | 大小 | 用途 |
|------|------|------|
| `企业招聘信息平台.exe` | 11MB | PE32+ 便携版，双击即用 |
| `企业招聘信息平台_安装包.exe` | 2.4MB | NSIS 安装向导，自动创建快捷方式 |
| `企业招聘信息平台.hta` | 101KB | 微信备用分发（不会拦截） |
| `中国企业招聘信息平台.zip` | 2.4MB | 安装包的压缩版（微信发这个） |

---

## 技术选型回顾

把很多技术的决策理由汇总在一处：

| 决策 | 选了什么 | 没选什么 | 为什么 |
|------|---------|---------|--------|
| 前端框架 | 原生 JS | React/Vue | 111 条静态数据不需要框架 |
| 桌面打包 | Tauri | Electron | 11MB vs 150MB，差 13 倍 |
| 图标格式 | SVG | PNG | 矢量 = 永不模糊 |
| 数据格式 | JS 内联 | JSON 文件 | 绕过 file:// CORS 限制 |
| 数据存储 | localStorage | 数据库 | 零依赖，同学拿到 .exe 就能用 |
| 链接器 | MSVC (VS 2022) | MinGW | 本机装了 VS，直接用 |
| 安装包 | NSIS | WiX | WiX 需要额外下载 .NET SDK |
| 分发 | .exe + .zip | 网页链接 | 同学不需要自己配置环境 |

---

## 如果以后想修改

1. **改数据**：编辑 `src/js/data.js`，按现有格式添加公司
2. **改样式**：编辑 `src/css/style.css`，改 CSS 变量就改了全局配色
3. **改文案**：编辑 `src/index.html`
4. **改交互逻辑**：编辑 `src/js/app.js`，注意所有数据走 `filterAndRender()`
5. **重新构建**：运行 `build.bat`
